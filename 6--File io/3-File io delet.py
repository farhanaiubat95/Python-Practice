#File i/o - delete
#using the os module
#Module (Like a code library) is a file written by another programmer that generally has
#a functions we can use
import os
os.remove("file name.txt") #it will be deleted